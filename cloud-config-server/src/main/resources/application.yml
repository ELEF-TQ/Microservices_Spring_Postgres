server:
  port: 5050

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ELEF-TQ/Config_Server
          clone-on-start: true